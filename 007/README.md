# 007 · GPU Bilateral Filter

| 难度 | Lv.2 | 语言 | C++ CUDA |
| 内容 | 保边双边滤波；共享内存 + 纹理内存 |
| 收获 | 纹理内存；与高斯模糊对比 |

```bash
mkdir -p build && cd build && cmake .. && make -j && ./bilateral
# 或: make -f Makefile
```
